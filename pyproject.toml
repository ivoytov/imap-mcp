[project]
name = "imap-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "markdownify>=1.2.2",
    "mcp==2.0.0",
]

[project.scripts]
imap-mcp = "imap_mcp.server:main"
