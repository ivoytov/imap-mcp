[project]
name = "imap-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp>=1.27.0",
]

[project.scripts]
imap-mcp = "imap_mcp.server:main"